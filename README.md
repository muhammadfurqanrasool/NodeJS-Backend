This is a NODEJS RESTAPI for an e-commerce web application.

To set it up, clone it, then:

1) Make a .env file and add the following variables: PORT, MONGO_URI
2) Type the command in CMD: npm install
3) To run, type: npm start
4) To run this, you must have mongodb installed in the system
5) 
